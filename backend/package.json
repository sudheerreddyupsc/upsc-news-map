{
  "name": "upsc-news-map-backend",
  "version": "1.0.0",
  "description": "Fetches news, classifies UPSC relevance with Gemini, geocodes, saves to Supabase. Run on a schedule via GitHub Actions.",
  "type": "module",
  "main": "fetch_and_classify.js",
  "scripts": {
    "start": "node fetch_and_classify.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "node-fetch": "^3.3.2"
  }
}
